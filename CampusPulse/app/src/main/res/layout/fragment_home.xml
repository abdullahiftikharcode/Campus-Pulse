<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".HomeFragment">

    <!-- RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/recyclerView"
        android:layout_marginTop="8dp"
        android:scrollbars="vertical" />

    <!-- Transparent Background View -->
    <View
        android:id="@+id/transparentBg"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:background="#66000000"
        android:visibility="invisible" />

    <!-- Floating Action Buttons and TextViews -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/fabConstraint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minHeight="32dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true">

        <!-- Main Floating Action Button -->

        <!-- Gallery Button -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/mainFabBtn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:tint="@color/purple"
            app:backgroundTint="@color/purple"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.953"
            app:layout_constraintStart_toStartOf="parent"
            app:srcCompat="@drawable/baseline_add_24"
            app:tint="@color/white" />

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/joinclub"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:tint="@color/purple"
            android:visibility="invisible"
            app:backgroundTint="@color/purple"
            app:layout_constraintBottom_toTopOf="@id/mainFabBtn"
            app:layout_constraintEnd_toEndOf="@id/mainFabBtn"
            app:srcCompat="@drawable/join"
            app:tint="@color/white" />

        <!-- Gallery Text -->
        <TextView
            android:id="@+id/jointxt"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/fab_text_bg"
            android:elevation="8dp"
            android:paddingHorizontal="32dp"
            android:paddingVertical="8dp"
            android:text="Join Club"
            android:textColor="@color/black"
            android:textStyle="bold"
            android:visibility="invisible"
            app:layout_constraintBottom_toBottomOf="@id/joinclub"
            app:layout_constraintEnd_toStartOf="@id/joinclub"
            app:layout_constraintTop_toTopOf="@id/joinclub" />

        <!-- Share Button -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/createClub"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:tint="@color/purple"
            android:visibility="invisible"
            app:backgroundTint="@color/purple"
            app:layout_constraintBottom_toTopOf="@id/joinclub"
            app:layout_constraintEnd_toEndOf="@id/joinclub"
            app:srcCompat="@drawable/create"
            app:tint="@color/white" />

        <!-- Share Text -->
        <TextView
            android:id="@+id/createtxt"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/fab_text_bg"
            android:elevation="8dp"
            android:paddingHorizontal="32dp"
            android:paddingVertical="8dp"
            android:text="Create Club"
            android:textColor="@color/black"
            android:textStyle="bold"
            android:visibility="invisible"
            app:layout_constraintBottom_toBottomOf="@id/createClub"
            app:layout_constraintEnd_toStartOf="@id/createClub"
            app:layout_constraintTop_toTopOf="@id/createClub" />

        <!-- Send Button -->

        <!-- Send Text -->
    </androidx.constraintlayout.widget.ConstraintLayout>

</RelativeLayout>
